import React from 'react';
import PropTypes from 'prop-types';

export default function Flloyd(props) {
  // var cnt = 0;
  //
  // for (let i = 0; i < props.height; i++) {
  //     for (let j = 0; j < props.weight; j++) {
  //         if (props.grid[i][j] === 1)
  //             props.graph[i][j] = 500000;
  //     }
  // }
  // console.log(props.graph);
  // for (let i = 0; i < props.height; i++) {
  //     for (let j = 0; j < props.weight; j++) {
  //         var ele = [cnt, props.grid[i][j]];
  //         props.map.push(ele);
  //         props.SPD[i][j] = props.graph[i][j];
  //         cnt++;
  //     }
  // }
  // let V=20;
  // for (let k = 0; k < V; k++) {
  //     // Pick all vertices as source one by one
  //     for (let i = 0; i < V; i++) {
  //         // Pick all vertices as destination for the
  //         // above picked source
  //         for (let j = 0; j < V; j++) {
  //             // If vertex k is on the shortest path from
  //             // i to j, then update the value of dist[i][j]
  //             if (props.SPD[i][k] + props.SPD[k][j] < props.SPD[i][j])
  //                 props.SPD[i][j] = props.SPD[i][k] + props.SPD[k][j];
  //         }
  //     }
  // }
  // console.log(props.SPD);
  return (

    <p>ok</p>
  );
};


